aws-lambda-powertools[all]>=2.0.0
boto3>=1.34.0
pdfplumber>=0.11.0
pypdf>=4.0.0
python-docx>=1.0.0
openpyxl>=3.1.0
# Required for PPTX upload-time conversion (shared/ingest._convert_pptx).
# Mirrors sdpm/pyproject.toml because we copy sdpm/sdpm into the Lambda
# bundle without pip-installing the package itself.
python-pptx>=1.0.0
lxml>=5.0.0
Pillow>=10.0.0
qrcode>=7.4.0
pygments>=2.19.2
defusedxml>=0.7.0
